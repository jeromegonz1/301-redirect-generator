# Fire Snake 301 - Configuration de fallback (HTTP uniquement)
# À utiliser temporairement si les certificats SSL ne sont pas disponibles

# Upstream definition for Streamlit
upstream streamlit_backend {
    server streamlit:8501;
    keepalive 32;
}

# HTTP server (fallback)
server {
    listen 80;
    listen [::]:80;
    server_name fire-snake-301.fr www.fire-snake-301.fr;
    
    # Streamlit WebSocket configuration
    location / {
        proxy_pass http://streamlit_backend;
        proxy_http_version 1.1;
        
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Timeouts for long-running requests
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
        
        # Disable buffering for streaming
        proxy_buffering off;
        proxy_cache off;
        
        proxy_redirect off;
    }

    # Streamlit specific endpoints
    location /_stcore/stream {
        proxy_pass http://streamlit_backend/_stcore/stream;
        proxy_http_version 1.1;
        
        # WebSocket headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # Disable buffering for WebSocket
        proxy_buffering off;
        proxy_cache off;
        
        # Extended timeouts for WebSocket
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }

    # Health check endpoint
    location /_stcore/health {
        proxy_pass http://streamlit_backend/_stcore/health;
        access_log off;
    }
}